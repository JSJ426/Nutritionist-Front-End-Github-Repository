export type AdditionalMenuItem = {
  id: number;
  title: string;
  category: string;
  description: string;
  date: string;
};

export type AdditionalMenuDraft = {
  title: string;
  category: string;
  description: string;
};
